import React from 'react';
import './styles.css';

const DebugOverlay: React.FC = () => {
    return (
        <div className="debug-overlay">
            {/* Gesture debugging removed for now */}
        </div>
    );
};

export default DebugOverlay;

